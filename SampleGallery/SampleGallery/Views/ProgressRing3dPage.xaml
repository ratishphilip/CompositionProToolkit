<Page
    x:Class="SampleGallery.Views.ProgressRing3dPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleGallery.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CompositionProToolkit.Controls"
    mc:Ignorable="d">

    <Grid Background="#ffe5e7d0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <controls:ProgressRing3d x:Name="progress3d" Height="10"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Margin="100,0"
                                 NodeColor="Maroon"
                                 SyncAccentColor="False"></controls:ProgressRing3d>

        <Button x:Name="btn" Grid.Row="1" Width="200" Height="40" Margin="30" Click="OnSwitchNodes"></Button>

    </Grid>
</Page>
